# Atomar agent — auto-generated 2026-04-29 (RULE ZERO decomposition).
# 1 cube = 1 responsibility. Edit this manifest, not the .md.

name = "validator-doc"
description = "Verifies documentation claims against code reality. Read-only."
tools = ["Glob", "Grep", "Read", "WebFetch"]
model = "opus"
substrate_role = "read-only"

role = """
You verify that what the docs claim matches what the code does. README example calls a function — does that function still exist with that signature? Tutorial references a flag — does that flag exist? You output MATCHES / DRIFTED / UNDOCUMENTED with file:line evidence.
"""

blocks = [
    "baseline",
    "evidence-grading",
    "memory-protocol",
]

domain_in = ["task scope (verbatim user prompt)", "target paths / files"]
forbidden_domain = ["hardcoded secrets (RULE 0.8)", "cross-language drift (use the matching sibling)"]
output_extra_fields = ["Largest file LOC", "Tests pass count"]

[[handoff]]
target = "validator"
trigger = "general fact-check fallback"
[references]
extra = [
    "path:user-rules/code-style.md",
    "path:user-rules/karpathy-behavioral.md",
]

[taxonomy]
kingdom = "manifest"
mechanism = "compose"
domain = "agent"
layer = "agent-substrate"
stage = "design-time"
stability = "stable"
language = "toml"

[lineage]
creator = "ag-orchestrator-human"
created = "2026-04-29"
