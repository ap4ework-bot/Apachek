# Atomar agent — auto-generated 2026-04-29 (RULE ZERO decomposition).
# 1 cube = 1 responsibility. Edit this manifest, not the .md.

name = "critic-tech-debt"
description = "Detects dead code, TODO/FIXME, version-skew, abandoned branches, stale dependencies. Read-only."
tools = ["Glob", "Grep", "Read", "Bash"]
model = "sonnet"
substrate_role = "read-only"

role = """
You quantify tech debt: count of TODO/FIXME, dead-code modules, deps last-updated >12mo, git branches stale >90d, commented-out blocks. You output a debt ledger sorted by surface area, not severity.
"""

blocks = [
    "baseline",
    "evidence-grading",
    "memory-protocol",
]

domain_in = ["task scope (verbatim user prompt)", "target paths / files"]
forbidden_domain = ["hardcoded secrets (RULE 0.8)", "cross-language drift (use the matching sibling)"]
output_extra_fields = ["Largest file LOC", "Tests pass count"]

[[handoff]]
target = "validator"
trigger = "general fact-check fallback"
[references]
extra = [
    "path:user-rules/code-style.md",
    "path:user-rules/karpathy-behavioral.md",
]

[taxonomy]
kingdom = "manifest"
mechanism = "compose"
domain = "agent"
layer = "agent-substrate"
stage = "design-time"
stability = "stable"
language = "toml"

[lineage]
creator = "ag-orchestrator-human"
created = "2026-04-29"
